# v6.1.0 Google OAuth Production Smoke Report

- 日期：2026-08-08（Asia/Taipei）
- 測試入口：LINE Windows app，「綠脈 AI 助理」
- 唯一前綴：`SMOKE-20260808-213735`
- Production deployment：`dpl_EsvDqPtfVoxzY6zKVifrPgLptqsh`，READY
- Production URL：<https://gpt-ai-assistant-mauve-seven.vercel.app>
- 版本：Production root 與 latest 都回 `6.1.0`

## Round-trip

- LINE 傳送一次 `連結 Google 行事曆`，bot 回覆 Google 授權卡；維護者只接手 Google 登入／同意步驟，完成後 AI 繼續驗收。
- 建立一筆 2026-08-08 22:22–22:37（Asia/Taipei）測試行程，只按一次確認；LINE 最終回覆「已同步到 Google 行事曆」。
- Google Calendar 以 22:15–22:45 緊縮時間窗與唯一前綴查到恰好一筆，開始／結束時間正確。
- Supabase SQL Editor 查到 `google-calendar-sync`、`google-calendar-status`、`line-reminder` 各一筆：sync／status jobs 均為 `done` 且 attempts 各 1，status job 已 delivery checkpoint；reminder job 已排入，之後隨 Google event 刪除取消為 `done`、attempts 0，未宣稱提醒曾送達。本輪時間窗 dead jobs 為 0。
- Vercel 近 20 分鐘 logs 包含 OAuth callback 303、complete 200、schedule-parse done、webhook 200 與每分鐘 Cron 200；5xx 為 0，timeout 為 0。
- 穩定網域 `/health/live` 回 `200 {"status":"OK"}`，root 回 200。

## Cleanup

- Google Calendar 測試 event 以精準 provider event id 刪除；相同前綴與時間窗重查為 0。
- Google inbound 已移除本地 `events` row；Supabase 先以唯一 title、provider id、confirmation 與 13:37–13:41 UTC 緊縮時間窗交叉定位同批資料。
- 以單一 CTE 精準刪除 3 筆測試 jobs 與 1 筆 confirmation，實際回傳 `deleted_jobs=3`、`deleted_confirmations=1`。
- 清理後重查：remaining events 0、confirmations 0、jobs 0、pending orphan line reminders 0、recent dead jobs 0。
- 本輪建立的無憑證 Temp probe script 與遮罩 env Temp 目錄均已刪除並複核不存在。LINE 聊天與 Vercel／Supabase 平台 logs 屬不可完整控制的稽核紀錄，保留並未宣稱毫無痕跡。
- `processed_events` 是 webhook durable dedupe authority，`runs` 是不含對話內容的運維證據；兩者未列入一般測試清理，避免破壞重送去重或刪掉必要 evidence。

## Operational Findings

- 已登入 Supabase Dashboard SQL Editor 是 Production DB 驗證的權威 UI 路徑；目前 Vercel Sensitive env 不能假設可由 `vercel env run` 注入，`vercel env pull` 也可能只給遮罩 placeholder。
- Chrome 擴充功能 UI 若遮住頁面會讓控制層暫停；關閉彈窗後應重新連線並續做，不代表 Supabase 帳號、專案或權限不可用。
- Google 登入／同意由維護者接手後，AI 必須從現有 LINE／Chrome 狀態主動續做行程、Google、Supabase、Vercel 與清理，不得把 OAuth 完成當成整輪驗收終點。
